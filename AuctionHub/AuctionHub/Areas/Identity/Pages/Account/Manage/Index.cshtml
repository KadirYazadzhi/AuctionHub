@page
@model IndexModel
@{
    ViewData["Title"] = "Profile";
    ViewData["ActivePage"] = ManageNavPages.Index;
}

<h3 class="fw-bold mb-4">@ViewData["Title"]</h3>
<partial name="_StatusMessage" for="StatusMessage" />

<!-- Stats Dashboard -->
<div class="row g-3 mb-5 animate-fade-up">
    <div class="col-6 col-lg-3">
        <div class="card border-0 shadow-sm rounded-4 bg-primary bg-gradient text-white h-100 transition-all hover-lift">
            <div class="card-body p-4 text-center text-lg-start">
                <div class="d-lg-flex align-items-center justify-content-between mb-2">
                    <small class="text-white-50 text-uppercase fw-bold letter-spacing-1">Active Bids</small>
                    <i class="bi bi-hammer fs-4 opacity-50 d-none d-lg-block"></i>
                </div>
                <h2 class="fw-bold mb-0">@Model.ActiveBidsCount</h2>
            </div>
        </div>
    </div>
    <div class="col-6 col-lg-3">
        <div class="card border-0 shadow-sm rounded-4 bg-success bg-gradient text-white h-100 transition-all hover-lift">
            <div class="card-body p-4 text-center text-lg-start">
                <div class="d-lg-flex align-items-center justify-content-between mb-2">
                    <small class="text-white-50 text-uppercase fw-bold letter-spacing-1">Win Rate</small>
                    <i class="bi bi-trophy fs-4 opacity-50 d-none d-lg-block"></i>
                </div>
                <h2 class="fw-bold mb-0">@Model.WinRate%</h2>
            </div>
        </div>
    </div>
    <div class="col-6 col-lg-3">
        <div class="card border-0 shadow-sm rounded-4 bg-white h-100 transition-all hover-lift border-start border-4 border-info">
            <div class="card-body p-4 text-center text-lg-start">
                <div class="d-lg-flex align-items-center justify-content-between mb-2">
                    <small class="text-muted text-uppercase fw-bold letter-spacing-1">Total Spent</small>
                    <i class="bi bi-cart-check text-info fs-4 opacity-50 d-none d-lg-block"></i>
                </div>
                <h2 class="fw-bold mb-0 text-dark">@Model.TotalSpent.ToString("C0")</h2>
            </div>
        </div>
    </div>
    <div class="col-6 col-lg-3">
        <div class="card border-0 shadow-sm rounded-4 bg-white h-100 transition-all hover-lift border-start border-4 border-warning">
            <div class="card-body p-4 text-center text-lg-start">
                <div class="d-lg-flex align-items-center justify-content-between mb-2">
                    <small class="text-muted text-uppercase fw-bold letter-spacing-1">Earnings</small>
                    <i class="bi bi-cash-stack text-warning fs-4 opacity-50 d-none d-lg-block"></i>
                </div>
                <h2 class="fw-bold mb-0 text-dark">@Model.TotalEarned.ToString("C0")</h2>
            </div>
        </div>
    </div>
</div>

<form id="profile-form" method="post" enctype="multipart/form-data">
    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
    
    <div class="row g-4 mb-4">
        <div class="col-md-4 text-center">
            <div class="position-relative d-inline-block">
                <div class="avatar-xl rounded-circle overflow-hidden shadow-sm border border-4 border-white bg-light" style="width: 150px; height: 150px;">
                    @if (!string.IsNullOrEmpty(Model.CurrentProfilePictureUrl))
                    {
                        <img src="@Model.CurrentProfilePictureUrl" class="w-100 h-100 object-fit-cover" alt="Profile" />
                    }
                    else
                    {
                        <div class="w-100 h-100 d-flex align-items-center justify-content-center text-secondary">
                            <i class="bi bi-person-fill display-1"></i>
                        </div>
                    }
                </div>
                <label for="profileUpload" class="btn btn-primary btn-sm rounded-circle position-absolute bottom-0 end-0 mb-2 me-2 shadow-sm" style="width: 32px; height: 32px; padding: 0; display: flex; align-items: center; justify-content: center;" title="Upload new picture">
                    <i class="bi bi-camera-fill"></i>
                </label>
                <input asp-for="Input.ProfilePicture" id="profileUpload" type="file" class="d-none" accept=".jpg,.jpeg,.png,.webp" onchange="previewImage(this)" />
            </div>
            <div class="mt-2">
                <span class="text-muted small">Allowed: JPG, PNG, WEBP (Max 2MB)</span>
                <span asp-validation-for="Input.ProfilePicture" class="text-danger d-block small"></span>
            </div>
        </div>

        <div class="col-md-8">
            <div class="row g-3">
                <div class="col-12 mb-2">
                    <label asp-for="Username" class="form-label small text-muted text-uppercase fw-bold"></label>
                    <input asp-for="Username" class="form-control bg-light" disabled />
                </div>
                
                <div class="col-12 mb-2">
                    <label asp-for="Input.PhoneNumber" class="form-label small text-muted text-uppercase fw-bold"></label>
                    <input asp-for="Input.PhoneNumber" class="form-control" placeholder="Phone number" />
                    <span asp-validation-for="Input.PhoneNumber" class="text-danger"></span>
                </div>

                <div class="col-md-6 mb-2">
                    <label asp-for="Input.FirstName" class="form-label small text-muted text-uppercase fw-bold"></label>
                    <input asp-for="Input.FirstName" class="form-control" placeholder="First name" />
                    <span asp-validation-for="Input.FirstName" class="text-danger"></span>
                </div>

                <div class="col-md-6 mb-2">
                    <label asp-for="Input.LastName" class="form-label small text-muted text-uppercase fw-bold"></label>
                    <input asp-for="Input.LastName" class="form-control" placeholder="Last name" />
                    <span asp-validation-for="Input.LastName" class="text-danger"></span>
                </div>

                <div class="col-12 mb-2">
                    <label asp-for="Input.AboutMe" class="form-label small text-muted text-uppercase fw-bold"></label>
                    <textarea asp-for="Input.AboutMe" class="form-control" rows="3" placeholder="Tell other users about yourself or what you offer..."></textarea>
                    <span asp-validation-for="Input.AboutMe" class="text-danger"></span>
                </div>
            </div>
        </div>
    </div>

    <div class="mt-4 text-end">
        <button id="update-profile-button" type="submit" class="btn btn-primary btn-lg rounded-pill px-5 shadow-sm">Save Changes</button>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function previewImage(input) {
            if (input.files && input.files[0]) {
                var reader = new FileReader();
                reader.onload = function(e) {
                    var img = document.querySelector('.avatar-xl img');
                    if (!img) {
                        // If no image existed (default icon), create one
                        var container = document.querySelector('.avatar-xl');
                        container.innerHTML = '<img src="" class="w-100 h-100 object-fit-cover" alt="Profile" />';
                        img = container.querySelector('img');
                    }
                    img.src = e.target.result;
                }
                reader.readAsDataURL(input.files[0]);
            }
        }
    </script>
}